<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fi.tuni.parsta">
    <uses-permission android:name="android.permission.VIBRATE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AchievementActivity"/>
        <activity android:name=".LevelsActivity"/>
        <activity android:name=".Game"/>
        <activity android:name=".AnswerResultActivity"/>
        <activity android:name=".CardItemContentsActivity"
            android:theme="@style/DialogTheme" />
        <activity android:name=".LevelDebriefActivity"
            android:theme="@style/DialogTheme" />
    </application>

</manifest>